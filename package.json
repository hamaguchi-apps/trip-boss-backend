{
  "name": "trip-boss-backend",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --concurrency=15",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "test": "turbo test"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.2.0",
  "engines": {
    "node": ">=20"
  }
}
